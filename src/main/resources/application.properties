# 1. MySQL ??
spring.datasource.url=jdbc:mysql://localhost:3306/practice?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
# 1. ?????? ?? (???? ???!)
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.hibernate.ddl-auto=update


spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# ? [??] JWT ??
# jwt.issuer: ?? ??? (???? ?? ?)
jwt.issuer=ajufresh@gmail.com
# jwt.secret_key: ??? (?? ?? ???? ?? ?. ?? ??!)
jwt.secret_key=study-springboot-backend-jwt-secret-key-change-it-later
# ?? ??? ??
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email